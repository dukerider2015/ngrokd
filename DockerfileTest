#
# Dockerfile for ngrokd
#

FROM debian:jessie
MAINTAINER kev <noreplay@easypi.info>

RUN apt-get update \
    && apt-get install -y build-essential \
                          curl \
                          git \
                          golang \
                          mercurial \

